# -----------------------------------------------------------------------------
# D&D Mapp: API Gateway - Environment Configuration Template
# -----------------------------------------------------------------------------

# === REQUIRED CONFIGURATION ===

# --- Database ---
# The password for the MariaDB user (Always required)
DB_PASS="pass"

# --- SSL Configuration ---
# Local SSL certificates are required for HTTPS/Fastify support in this gateway.
# Use `pnpm gen:ssl-cert` to generate these via mkcert.
SSL_KEY_PATH="./ssl-key.pem"
SSL_CERT_PATH="./ssl-cert.pem"


# === OPTIONAL / DEFAULT CONFIGURATION ===

# --- Server ---
# HOST: The hostname the gateway binds to
HOST="localhost.api.dndmapp.dev"

# PORT: The port the gateway listens on
PORT="4450"

# --- Database Details ---
DB_USER="root"
DB_HOST="localhost"
DB_PORT="3306"
DB_NAME="dma_api_dev"

# DATABASE_URL is constructed from the components above
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://${DB_USER:-"user"}:${DB_PASS:-"pass"}@${DB_HOST:-"localhost"}:${DB_PORT:-"3306"}/${DB_NAME:-"dma_api_dev"}

# --- Security & CORS ---
# Comma-separated list of allowed origins (No spaces)
CORS_ORIGINS="*"
